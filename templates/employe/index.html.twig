{% extends 'base.html.twig' %}

{% block title %}Liste des Employés {% endblock %}

{% block body %}
<h1>Liste des Employés </h1>
{% for employe in employes %}
    <a href="{{path('show_employe',{'id':employe.id}) }} ">{{employe ~ ' ' ~ employe.entreprise.raisonSociale}}</a> 
    <a class="btn btn-warning" href="{{path('edit_employe',{'id':employe.id}) }} ">Edit</a>
    <a class="btn btn-danger" href="{{path('delete_employe',{'id':employe.id})}}">supp</a>
    <br>
{% endfor %}
<a class="btn btn-success" href="{{path('new_employe')}}">Ajouter</a>
{% endblock %}
 